/* Rally Point News — progressive enhancement for the news command center. */
(async()=>{
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const age=d=>{if(!d)return'';const m=Math.max(0,Math.floor((Date.now()-new Date(d).getTime())/60000));return m<60?`${m}m ago`:m<1440?`${Math.floor(m/60)}h ago`:`${Math.floor(m/1440)}d ago`};
 const norm=u=>{try{const x=new URL(u,location.href);x.hash='';return x.href.replace(/\/$/,'')}catch{return String(u||'')}};
 const track=(name,params={})=>{try{if(typeof window.gtag==='function')window.gtag('event',name,params)}catch(e){}};
 const image=b=>b.image||b.image_url||b.lead_image||b.hero_image||'';
 const importance=b=>{if(Number.isFinite(Number(b.importance_score)))return Number(b.importance_score);const t=` ${b.title||''} ${b.description||''} `.toLowerCase();let n=50;for(const [k,w] of Object.entries({'supreme court':18,'congress':15,'federal':12,'president':12,'election':14,'midterm':14,'ballot':13,'court':10,'war':18,'attack':14,'shipping':9,'oil':11,'market':8,'tariff':9,'emergency':10,'crash':12,'killed':12,'hurricane':10,'fema':10,'usps':8,'artificial intelligence':8,'crypto':6,'shutdown':10}))if(t.includes(k))n+=w;n+=Math.min(8,Math.max(0,Number(b.source_count||0)-2)*2);const d=Date.parse(b.published_at||'');if(Number.isFinite(d))n+=Math.max(0,12-Math.max(0,(Date.now()-d)/86400000)*2);return n};
 document.addEventListener('click',e=>{const a=e.target.closest('[data-rp-event],.newsletter-native-cta');if(!a)return;track(a.dataset.rpEvent||'link_click',{link_url:a.href||'',link_text:(a.textContent||'').trim().slice(0,100)})});
 document.querySelectorAll('.newsroom-nav a').forEach(a=>{const t=(a.textContent||'').trim();if(t==='AI Newsroom'){a.textContent='Rally Briefs';a.href='briefs/';a.dataset.rpEvent='rally_briefs_nav_click'}if(t==='The Wire'){a.textContent='Rally Wire';a.href='#grid'}if(t==='Source Monitor'){a.textContent='Rally Wire';a.href='#grid'}if(t==='Newsletter'){a.href='newsletter/';a.dataset.rpEvent='newsletter_nav_click'}});
 const section=document.querySelector('.section-label span');if(section&&(section.textContent.trim()==='The Wire'||section.textContent.trim()==='Source Monitor'))section.textContent='Rally Wire';const sectionLabel=document.querySelector('.section-label');if(sectionLabel){const small=sectionLabel.querySelector('small');if(small)small.textContent='Live headlines from across the source network'}
 const newsletter=document.querySelector('.newsletter-card');if(newsletter){newsletter.id='briefing';const legacy=newsletter.querySelector('iframe[src*="substack.com"]');if(legacy)legacy.replaceWith(Object.assign(document.createElement('a'),{href:'newsletter/',className:'newsletter-native-cta',textContent:'Subscribe free to The Rally Brief →'}));const h=newsletter.querySelector('h2');if(h)h.textContent='Get The Rally Brief';const p=newsletter.querySelector('p');if(p)p.textContent='A concise email built around Rally Point reporting and source links you can inspect.'}
 try{
  const br=await fetch(`data/briefs.json?t=${Date.now()}`,{cache:'no-store'});
  if(br.ok){const bd=await br.json(),briefs=(bd.briefs||[]).slice().sort((a,b)=>importance(b)-importance(a)).slice(0,6);if(briefs.length){
   document.querySelector('.ai-newsroom-home')?.remove();const wrap=document.createElement('section');wrap.className='ai-newsroom-home';wrap.setAttribute('aria-label','Rally Point reports ranked by importance');
   wrap.innerHTML=`<div class="ai-newsroom-head"><div><span>Rally Point Reports</span><h2>The stories that matter most.</h2><p>Original source-based reports positioned by news importance rather than simply publication time.</p></div><a href="briefs/" data-rp-event="rally_briefs_archive_click">All Rally Briefs →</a></div><div class="ai-newsroom-grid">${briefs.map((b,i)=>{const img=image(b),u=esc(b.url||'/briefs/'),title=esc(b.title||'Rally Point Report');return `<article class="ai-brief ${i===0?'ai-brief-lead':''}">${img?`<a class="ai-brief-image" href="${u}"><img src="${esc(img)}" alt="${esc(b.image_alt||b.title||'')}" width="1200" height="675" loading="${i===0?'eager':'lazy'}" decoding="async"></a>`:`<a class="ai-brief-image ai-brief-image-placeholder" href="${u}" aria-label="Read ${title}"><span>RALLY POINT</span><b>${i===0?'TOP REPORT':'RALLY BRIEF'}</b></a>`}<div class="ai-brief-content"><div class="ai-brief-kicker">${i===0?'Top Report':'Rally Brief'}${b.source_count?` · ${esc(b.source_count)} sources`:''}</div><h3><a href="${u}" data-rp-event="rally_brief_story_click">${title}</a></h3><p>${esc(b.description||'Original source-based reporting from Rally Point News.')}</p><a class="ai-brief-read" href="${u}">Read report →</a></div></article>`}).join('')}</div>`;
   const latest=document.querySelector('.latest-brief'),label=document.querySelector('.section-label');(latest||label)?.insertAdjacentElement('beforebegin',wrap);
  }}
 }catch(e){}
 try{
  const r=await fetch(`data/storylines.json?t=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw 0;const d=await r.json();const generated=new Date(d.generated_at||0);if(!d.storylines?.length||Date.now()-generated.getTime()>180*60000)throw 0;const multi=d.storylines.filter(x=>x.source_count>=2);if(!multi.length)return;
  let strip=document.querySelector('.newsroom-strip');if(!strip){strip=document.createElement('div');strip.className='newsroom-strip';strip.innerHTML=`<span><b>${d.storyline_count}</b> monitored storylines</span><span><b>${d.multi_source_count}</b> multi-source</span><span><b>${d.developing_count}</b> developing</span><span>Rally Wire</span>`;document.querySelector('header')?.insertAdjacentElement('afterend',strip)}
  const lead=multi[0],c=lead.coverage||[],first=c[0];if(first){const el=document.getElementById('lead');if(el)el.innerHTML=`<div class="kicker">Rally Wire · ${lead.status==='developing'?'Developing':'Major Story'}</div><div class="lead-body"><div class="storyline-proof"><strong>${lead.source_count} sources</strong> covering this storyline</div><h1><a href="${esc(first.link)}" target="_blank" rel="noopener">${esc(lead.title)}</a></h1><div class="source-tag">${esc(first.source)} <span class="dot">•</span><span class="time">${age(first.date)}</span></div><div class="also-list">${c.slice(1,5).map(x=>`<div class="also-item"><span class="also-source">${esc(x.source)}</span><a href="${esc(x.link)}" target="_blank" rel="noopener">${esc(x.title)}</a></div>`).join('')}</div></div>`}
  const count=document.getElementById('storyCount');if(count)count.textContent=`${d.storyline_count} monitored storylines · ${d.multi_source_count} covered across multiple sources`;const byLink=new Map();for(const s of multi)for(const item of(s.coverage||[]))byLink.set(norm(item.link),s);const decorate=()=>document.querySelectorAll('#grid .story').forEach(card=>{if(card.dataset.rpIntel==='1')return;const a=card.querySelector('h3 a');if(!a)return;const s=byLink.get(norm(a.href)),body=card.querySelector('.story-body');if(!s||!body)return;const badge=document.createElement('div');badge.className='storyline-proof wire-proof';badge.innerHTML=`<strong>${s.source_count} sources</strong><span>${s.status==='developing'?'developing':'multi-source'}</span>`;body.insertBefore(badge,body.firstChild);card.dataset.rpIntel='1'});decorate();const grid=document.getElementById('grid');if(grid)new MutationObserver(decorate).observe(grid,{childList:true,subtree:true});
 }catch(e){}
})();

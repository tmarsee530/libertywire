name: Rally Brief Delivery (retired)

# Rally Point's current product is the focused live news/link-ranking homepage.
# The former Rally Brief newsletter product is retired. Keep this file as an
# explicit tombstone so legacy automation is not accidentally re-enabled by
# repository archaeology; there are intentionally no automatic triggers/jobs.

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired product notice
        run: echo "Rally Brief delivery is retired under the current Rally Point product model."
